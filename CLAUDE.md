# Project Instructions

## Git

- Whenever you make a change in this repo, commit and push it (`git push`) to GitHub without asking for permission first — don't leave finished work sitting only in the local commit history or working tree.
- Never stage or commit the `Keys/` folder — it holds credentials, not project content.
