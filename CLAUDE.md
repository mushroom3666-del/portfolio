# Project Instructions

## Git

- Always push to GitHub (`git push`) after committing a change in this repo — don't leave finished work sitting only in the local commit history.
- Never stage or commit the `Keys/` folder — it holds credentials, not project content.
